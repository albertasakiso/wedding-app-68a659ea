## Continuation Plan — Cleanup + High-Impact FRS\n\nBuilding on what's already shipped (Hero post-wedding state, gift progress bars, scroll-reveal hook). This plan tackles the **remaining unconsolidated modules** and adds the **next batch of FRS** with highest value-to-effort ratio.\n\n---\n\n### Part A — Cleanup / Consolidation (Tech Debt)\n\n| # | Item | Action |\n|---|---|---|\n| A1 | Dead `payment-api` edge function | Delete `supabase/functions/payment-api/` and remove its entry from `supabase/config.toml` |\n| A2 | Unused DB columns | Migration to drop: `payment_settings.paystack_public_key`, `paystack_secret_key`, `stripe_public_key`, `stripe_secret_key`; `rsvps.meal_preference`, `rsvps.dietary_restrictions` |\n| A3 | `anonymizeEntry` misplaced | Create `src/lib/format-utils.ts`, move function, update imports in `MessagesWall.tsx` and anywhere else |\n| A4 | Realtime subscription duplication | Create `src/hooks/useRealtimeTable.ts` (subscribes to a Postgres changes channel + invokes callback). Refactor `Gifts.tsx` to use it for `gift_wall` + `gift_payments` |\n| A5 | Hardcoded admin tabs | Extract `ADMIN_TABS` config array in `AdminDashboard.tsx`, drive both `TabsList` and `TabsContent` from the array |\n\n---\n\n### Part B — FRS Implementation (New Features)\n\n**B1. Guest Photo Upload (Gallery)**\n- Add an \